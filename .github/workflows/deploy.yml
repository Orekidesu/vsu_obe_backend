# name: Deploy to EC2
# on:
#   push:
#     branches: [ main ]
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3
        
#       - name: Setup SSH
#         uses: webfactory/ssh-agent@v0.7.0
#         with:
#           ssh-private-key: ${{ secrets.EC2_SSH_KEY }}
          
#       - name: Add SSH known hosts
#         run: |
#           mkdir -p ~/.ssh
#           ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts
#       - name: Deploy to server
#         run: |
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && git pull origin main"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && rm -rf vendor composer.lock"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && composer install --no-dev --optimize-autoloader"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && php artisan migrate --force"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && php artisan config:cache"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && php artisan route:cache"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && php artisan view:cache"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && php artisan optimize"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && sudo chown -R www-data:www-data storage bootstrap/cache"
#           ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "cd ${{ secrets.DEPLOY_PATH }} && sudo chmod -R 775 storage bootstrap/cache"


name: Deploy to EC2
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}
          
      - name: Add SSH known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts
          
      - name: Deploy to server
        run: |
          ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} << 'EOF'
            cd ${{ secrets.DEPLOY_PATH }}

            # Pull latest changes
            git reset --hard HEAD
            git pull origin main || exit 1
            
            # Install dependencies
            rm -rf vendor composer.lock
            composer install --no-dev --optimize-autoloader
            php artisan migrate --force

            # 🛠️ Fix Laravel cache issues
            sudo -u www-data php artisan config:clear
            sudo -u www-data php artisan cache:clear
            sudo -u www-data php artisan route:clear
            sudo -u www-data php artisan view:clear
            
            sudo -u www-data php artisan config:cache
            sudo -u www-data php artisan route:cache
            sudo -u www-data php artisan view:cache
            sudo -u www-data php artisan optimize
            
            # 🔥 Ensure correct ownership and permissions
            sudo chown -R ubuntu:www-data .
            sudo chmod -R 775 .

            # 🔒 Specifically fix permissions for storage & cache (Laravel needs write access)
            sudo chmod -R 775 storage bootstrap/cache
            sudo chown -R www-data:www-data storage bootstrap/cache

            # Restart Apache to apply changes
            sudo systemctl restart apache2
          EOF


